events {}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
        }

        location /videos/ {
            alias /usr/share/nginx/html/videos/;
            autoindex on;
        }
    }
}
